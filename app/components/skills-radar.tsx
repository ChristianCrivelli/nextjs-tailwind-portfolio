// Superseded by ./skills-list.tsx (an icon-labeled list, grouped by
// category) — see app/page.tsx, which now renders <SkillsList /> instead of
// <SkillsRadar />. This file is no longer imported anywhere; it's kept only
// because this session can't delete files on your machine. Safe to delete
// this file yourself, e.g. `git rm app/components/skills-radar.tsx`.
export {};
